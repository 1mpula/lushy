
// This is a helper type if needed, but typically we import specific icons.
// For example: import { Heart } from 'lucide-react-native';
// <Heart color="#FF4081" size={24} />

// We can export common icons here if we want a centralized registry, 
// or just rely on direct imports in components.
// Given strict Atomic design, let's create a generic Icon atom wrapper if we strictly need one, 
// but usually direct usage of library icons is preferred in React Native.

export * from 'lucide-react-native';
